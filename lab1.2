#include <iostream>
#include <cmath>
using namespace std;
int main()
{
    setlocale(LC_ALL, "Russian");
    // Задание 1;
    cout << "Введите значение x" << endl;
    int x;
    cin >> x;
    cout << "Введите значение y" << endl;
    int y;
    cin >> y;
    cout << "Значение z равно" << endl << log10(pow(sin(x), 2)) / (abs(cos(y)) + 2) << endl;
    // Задание 2;
    cout << "Введите значение x1" << endl;
    double x1;
    cin >> x1;


    if (x1 < -2)
    {
        cout << pow(sin(6 * x1), 3) / (4 * x1 - pow(x1, 4));
    }
    else if ((-2 <= x1) && (x1 < 3))
    {
        cout << (2 * log(x1 - 1) - pow(exp(1), x1)) / (8 * x1 + pow(x1, 2));
    }
    else if (x1 >= 3)
    {
        cout << (((pow(x1, 3) * 2) + 4) / (pow(x1, 2) * 5 - 2)) * tan(2 * x1 + 1);
    }

}
